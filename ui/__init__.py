"""
ui 패키지 — PySide6 UI 전체.

하위 구조:
  constants.py  — 색상, 폰트, 크기 상수
  styles/       — QSS 테마 생성 (build_qss)
  widgets/      — 공용 커스텀 위젯 (LogWidget, FolderSelector)
  tabs/         — 탭별 UI 구현
  main_window.py — 메인 윈도우
"""
